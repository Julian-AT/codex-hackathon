---
phase: 01-identity-cleanup-baseline-and-migration-map
plan: "09"
type: tdd
wave: 8
depends_on:
  - 01-08
files_modified:
  - test/integration/validation-scripts.test.ts
  - test/integration/privacy-boundaries.test.ts
  - package.json
  - biome.json
  - .gitignore
  - vitest.config.ts
  - fixtures/phase-1/validation/adapter-tools.json
  - lib/data/schema-gate.test.ts
  - migration/phase-1-change-scope.v1.json
autonomous: true
requirements:
  - IDEN-08
  - IDEN-09
  - IDEN-10
must_haves:
  truths:
    - "All eight stable scripts exist exactly once and reach the Plan 08 production process/capability entry, a direct external Vitest path, an explicit LIVE FAIL product gate, or a named host probe; none is missing, recursive, aliased, or a successful no-op."
    - "check, typecheck, test, and test:integration pass after narrow scope/fixture repair; Studio, dataset, and benchmark remain owner-tagged LIVE FAIL; local check skips only for a named unavailable capability."
    - "The integration suite stubs its own descriptor when testing catalogs/baselines; only the external Plan 01 harness invokes live test:integration."
    - "Instrumented validation performs no private-repository enumeration/clone, credential/private-data read, upload, model/training launch, or operator-state access."
    - "Generated .next output is narrowly ignored by both Git and Biome without suppressing source paths or treating generated output as acceptance."
    - "The durable Phase 1 change manifest preserves the immutable Plan 01 baseline and operator-owned D src/app.tsx deletion verbatim, then appends final path/hunk ownership for Plans 01-09 only after all Phase 1 source/config/test work is complete."
  artifacts:
    - path: "package.json"
      provides: "Exactly eight stable validation script mappings to the internal validation CLI"
    - path: "test/integration/validation-scripts.test.ts"
      provides: "Script mapping, gate outcome, external-only recursion, and full-row acceptance proof"
    - path: "test/integration/privacy-boundaries.test.ts"
      provides: "Cross-slice no-egress/no-private-state denial proof"
    - path: ".gitignore"
      provides: "Narrow .next generated-output exclusion"
    - path: "migration/phase-1-change-scope.v1.json"
      provides: "Immutable preflight evidence plus final append-only Phase 1 path/hunk ownership and zero-owned-production-deletion proof"
  key_links:
    - from: "package.json"
      to: "src/validation/process-entry.ts"
      via: "each stable script selects one unique catalog check or explicit external integration mode; no script accepts arbitrary argv or invokes itself"
      pattern: "src/validation/process-entry.ts"
    - from: "test/integration/validation-scripts.test.ts"
      to: "external-only test:integration descriptor"
      via: "in-suite tests inject a fake result; the outer package path reaches Vitest directly through Plan 08's fixed external argv and never through `bun run test:integration` recursion"
      pattern: "external"
    - from: "biome.json"
      to: ".gitignore"
      via: "both narrowly exclude .next while retaining source and explicit generated evidence labeling"
      pattern: ".next"
    - from: "migration/phase-1-change-scope.v1.json"
      to: "the complete Phase 1 worktree after Plan 09"
      via: "append final plan/path/hunk ownership against the immutable baseline while preserving all pre-existing operator status, hashes, modes, link targets, and D src/app.tsx evidence verbatim"
      pattern: "baseline"
  prohibitions:
    - "Validation MUST NOT describe skip, replay, fixture, mock, unavailable shell, or absent product as passing live evidence."
    - "Validation MUST NOT recurse into test:integration, access private/operator state, publish, launch models/training, or accept arbitrary shell input."
---

## Phase Goal

**As an** operator or maintainer, **I want to** use one collision-safe MLX product surface and inspect an honest validation baseline plus complete replacement map, **so that** I can operate and evolve the brownfield system without ambiguous identity, unsafe takeover, or false acceptance.

<objective>
Wire the eight stable validation scripts to the production adapters, repair the portable baseline narrowly, prove truthful product/capability outcomes plus privacy at the real package/process boundary, and freeze final Phase 1 change ownership.

Purpose: Turn the isolated validation contracts into the stable repository acceptance interface without recursion, generated-output noise, mutable fixtures, or false live claims.
Output: Package/config/fixture wiring, external integration/privacy acceptance tests, and the final append-only Phase 1 change-scope manifest.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/SKELETON.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-SPEC.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-UI-SPEC.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-RESEARCH.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-PATTERNS.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-VALIDATION.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-08-SUMMARY.md
@package.json
@biome.json
@.gitignore
@vitest.config.ts
@lib/data/schema-gate.test.ts

<interfaces>
- Follow `01-PATTERNS.md` test-configuration/process-integration guidance: retain Node environment, explicit includes, serial determinism, isolated paths, and synthetic fixtures.
- Plan 07 provides `runValidationCli` and fake-port proof for external-only handling of `test:integration`.
- Plan 08 provides `createBunCheckRunner`, the named host capability probe, and `src/validation/process-entry.ts`; it wires fixed catalog argv to the host while refusing arbitrary commands.
- Integration tests inspect/stub the integration descriptor inside the suite. The outer `test/support/phase-1-gate.ts verify validation-acceptance` process invokes the package integration script once; that script enters explicit external mode and starts Vitest directly with the catalog's fixed argv.
</interfaces>
</context>

## Artifacts this phase produces

- New paths: `test/integration/validation-scripts.test.ts`, `test/integration/privacy-boundaries.test.ts`, and `fixtures/phase-1/validation/adapter-tools.json`.
- Modified configuration/evidence: `package.json`, `biome.json`, `.gitignore`, `vitest.config.ts`, and `migration/phase-1-change-scope.v1.json`.
- Stable scripts: `check`, `typecheck`, `test`, `test:integration`, `studio:build`, `dataset:validate`, `benchmark:smoke`, and `local:check`.

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1: RED — Specify script outcomes, recursion isolation, and privacy</name>
  <files>test/integration/validation-scripts.test.ts, test/integration/privacy-boundaries.test.ts</files>
  <read_first>
    - `01-SPEC.md` AC-19 through AC-24
    - Plan 06 descriptor policy and Plan 07 fake/external runner boundary
    - `01-RESEARCH.md` stable-script and privacy tables
    - `01-PATTERNS.md` integration/privacy fixture requirements
  </read_first>
  <behavior>
    - Package inspection finds eight unique mappings and no recursive/self alias.
    - In-suite descriptor tests inject a deterministic integration result and assert no live self-spawn.
    - Process-boundary assertions prove portable scripts reach the real Plan 08 runner/probe entry and external integration reaches direct fixed Vitest argv without accepting caller command tokens.
    - Product gates are nonzero LIVE FAIL with owners 8/5/6; local behavior requires named capability evidence.
    - Deny spies remain untouched for network, GitHub/HF, clone, upload, credentials, homes, mirrors, models, and training.
  </behavior>
  <acceptance_criteria>
    - Both files collect and fail behavior assertions for missing mappings/repairs; the Plan 01 gate rejects setup/import/collection/syntax/no-tests errors.
    - The RED command runs both files without short-circuiting and records each result.
    - No test invokes live test:integration from inside the integration process, and no mapping shells out through its own package-script name.
  </acceptance_criteria>
  <action>
Write package/script and privacy integration tests first. Inject fake execution for the self descriptor and instrument every prohibited seam. Assert that package mappings select Plan 08's process entry, that external integration uses its explicit mode, and that no mapping accepts arbitrary executable/argv or invokes a package-script name recursively. Use late-loading assertions for absent wiring so suites collect and fail on named behavior. Run through the Plan 01 gate and preserve both RED results.
  </action>
  <verify>
    <automated>bun test/support/phase-1-gate.ts expect-red validation-integration</automated>
  </verify>
  <done>The RED integration contract covers all eight outcomes, self-recursion isolation, source labels, owner phases, and privacy without executing itself.</done>
</task>

<task type="auto" gate="GREEN">
  <name>Task 2: GREEN — Wire scripts and narrow generated-output scope</name>
  <files>package.json, biome.json, .gitignore, vitest.config.ts</files>
  <read_first>
    - RED integration tests
    - Plan 06 catalog and Plan 07 internal CLI
    - Current package/Biome/Git/Vitest configuration
    - `01-PATTERNS.md` exact configuration analogs
  </read_first>
  <acceptance_criteria>
    - Eight scripts map uniquely to Plan 08's executable validation entry/check IDs with no dependency/version/unrelated-script changes; `test:integration` selects explicit external mode.
    - `.next/` is narrowly ignored in `.gitignore` and Biome; no broad source or private acceptance exclusion is added.
    - Unit/integration includes remain separate and bounded.
    - Mapping/recursion-focused integration assertions pass with the self descriptor stubbed.
  </acceptance_criteria>
  <action>
Wire the eight package scripts to unique catalog IDs through `src/validation/process-entry.ts`, preserve existing unrelated scripts/dependencies, and apply the exact `.next/` exclusions in Git and Biome. Map `test:integration` to explicit external mode so the process entry starts Vitest directly from the catalog's immutable argv; never call `bun run test:integration` from the entry or from its suite. Keep explicit unit/integration configs and ensure the in-suite descriptor path is stubbed rather than executed.
  </action>
  <verify>
    <automated>bun x vitest run --config vitest.integration.config.ts test/integration/validation-scripts.test.ts -t "maps all eight scripts without recursion" &amp;&amp; bun run typecheck</automated>
  </verify>
  <done>All script identities and generated-output scopes are wired safely, and recursion-focused assertions pass.</done>
</task>

<task type="auto" gate="REFACTOR">
  <name>Task 3: REFACTOR — Repair fixtures, prove privacy, and freeze final change ownership</name>
  <files>fixtures/phase-1/validation/adapter-tools.json, lib/data/schema-gate.test.ts, test/integration/validation-scripts.test.ts, test/integration/privacy-boundaries.test.ts, migration/phase-1-change-scope.v1.json</files>
  <read_first>
    - GREEN script/config wiring
    - Mutable manifest failure described in `01-RESEARCH.md`
    - Plan 01 external gate contract
    - Plan 01 pre-edit change-scope manifest and every Plan 01-09 summary/diff; preserve unrelated package/config/test hunks and the operator-owned deletion verbatim
  </read_first>
  <acceptance_criteria>
    - Schema-gate tests use the committed synthetic FIXTURE file rather than mutable repository data.
    - Focused schema-fixture, privacy, and final change-ownership assertions pass in under the task-level feedback budget before the full acceptance harness runs at plan level.
    - The final change manifest preserves the entire immutable baseline and the operator-owned `D src/app.tsx` status/hashes/hunks verbatim, then appends every Plan 01-09 path and overlapping hunk only after all source/config/test edits exist.
    - Final ownership contains zero Phase 1-owned legacy production source/runtime deletion; no pre-existing operator status or hunk is reclassified, restored, staged, or omitted.
  </acceptance_criteria>
  <action>
Move the mutable adapter-tools assumption to the committed synthetic fixture and finalize focused integration/privacy assertions under a test title containing `fixture, privacy, and final change ownership`. After every Plan 01-09 source/config/test edit exists, append final plan/path/hunk ownership to the pre-edit change-scope manifest. Treat the baseline section as immutable bytes, carry the recorded operator-owned `D src/app.tsx` deletion verbatim, distinguish overlapping operator/phase hunks, and reject any Phase 1-owned production source/runtime deletion. Run the focused fixture/privacy/ownership checks first. The plan-level acceptance gate then samples every portable and product/capability script independently from outside the integration process, validates semantic output and exits, and never stops after the first failure.
  </action>
  <verify>
    <automated>bun x vitest run lib/data/schema-gate.test.ts &amp;&amp; bun x vitest run --config vitest.integration.config.ts test/integration/validation-scripts.test.ts test/integration/privacy-boundaries.test.ts -t "fixture, privacy, and final change ownership"</automated>
  </verify>
  <done>The mutable fixture defect is isolated, privacy boundaries pass, and the durable manifest contains complete final Phase 1 ownership with the immutable operator baseline preserved and zero Phase 1-owned production deletion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Package scripts → validation CLI | Public automation names can recurse, alias, or bypass typed checks. |
| Validation → operator environment | Checks could reach private repositories, credentials, state, network, publication, models, or training. |
| Generated/mutable files → baseline | Build output or mutable data can create nondeterministic or false acceptance. |
| Dirty worktree → final change manifest | Phase-owned hunks can be confused with pre-existing operator edits or deletions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-38 | Denial of Service | `test:integration` mapping | high | mitigate | External-only descriptor, in-suite stub, and one outer harness invocation. |
| T-01-39 | Information Disclosure | privacy integration | high | mitigate | Deny spies for repositories, credentials, homes, uploads, models, training, and network. |
| T-01-40 | Spoofing | product/capability scripts | high | mitigate | Exact exit/status/source/owner/capability assertions; no successful no-op. |
| T-01-41 | Tampering | generated/mutable baseline inputs | medium | mitigate | Narrow `.next/` ignores and committed synthetic schema fixture. |
| T-01-42 | Repudiation | `migration/phase-1-change-scope.v1.json` | high | mitigate | Immutable preflight bytes plus final path/hunk ownership, explicit overlap attribution, and verbatim preservation of the operator-owned deletion. |
</threat_model>

<verification>
- `bun test/support/phase-1-gate.ts verify validation-acceptance`
- Inspect all eight mappings: unique IDs, no self-recursion, no dependency or unrelated-script rewrite.
- Require the outer integration path to reach Vitest directly through Plan 08's fixed executable/argv; in-suite catalog/baseline paths retain the injected external-descriptor stub.
- Compare every final Phase 1 path/hunk with the immutable Plan 01 baseline, preserve `D src/app.tsx` and all other operator-owned evidence verbatim, and require zero Phase 1-owned production source/runtime deletion.
</verification>

<success_criteria>
- Four portable scripts pass through real tools and deterministic fixtures.
- Three absent-product scripts are owner-tagged LIVE FAIL; local skip requires named unavailable capability.
- The external harness samples every command without short-circuiting and test:integration never recurses.
- Privacy/egress/model/training/operator-state denies remain untouched.
- `.next/` and the mutable manifest defect are repaired narrowly and honestly.
- The final change-scope manifest covers Plans 01-09 after all edits, preserves immutable preflight/operator evidence, and proves AC-18 with path/hunk ownership rather than a whole-worktree deletion count.
</success_criteria>

<output>
Create `.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-09-SUMMARY.md` when done.
</output>
