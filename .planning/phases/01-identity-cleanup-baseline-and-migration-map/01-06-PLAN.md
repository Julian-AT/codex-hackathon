---
phase: 01-identity-cleanup-baseline-and-migration-map
plan: "06"
type: tdd
wave: 5
depends_on:
  - 01-05
files_modified:
  - src/validation/result.test.ts
  - src/validation/capabilities.test.ts
  - src/validation/check-catalog.test.ts
  - src/validation/result.ts
  - src/validation/capabilities.ts
  - src/validation/check-catalog.ts
autonomous: true
requirements:
  - IDEN-09
  - IDEN-10
must_haves:
  truths:
    - "Every normalized check has exactly one PASS, FAIL, or SKIP plus an independent LIVE, REPLAY, or FIXTURE source; malformed, missing, empty, contradictory, and unknown inputs fail closed."
    - "SKIP is valid only with a named probe that reports a genuinely unavailable host capability; missing implementation, owner phase, fixture, replay, or mock evidence cannot produce PASS or LIVE."
    - "One ordered catalog declares all eight stable script/check identities, expected evidence source, immutable allowlisted executable arguments or typed product/capability gate, and external-only execution policy."
    - "The test:integration descriptor is marked for an external harness and has an injected stub path for catalog/baseline tests, so validation cannot recursively execute its own integration suite."
  artifacts:
    - path: "src/validation/result.ts"
      provides: "Schema, fail-closed normalization, row retention, and failure-dominant aggregation"
      exports: ["ValidationStatus", "EvidenceSource", "ValidationResultInput", "ValidationResult", "normalizeValidationResult", "aggregateValidationResults"]
    - path: "src/validation/capabilities.ts"
      provides: "Named host capability evidence and SKIP eligibility"
      exports: ["CapabilityEvidence", "normalizeCapabilityEvidence", "canSkipForCapability"]
    - path: "src/validation/check-catalog.ts"
      provides: "Ordered eight-command descriptor registry with fixed arguments, owner gates, capability metadata, and execution policy"
      exports: ["CheckDescriptor", "CHECK_CATALOG", "getCheckDescriptor"]
  key_links:
    - from: "src/validation/capabilities.ts"
      to: "src/validation/result.ts"
      via: "SKIP normalization requires named unavailable capability evidence and never changes source classification"
      pattern: "canSkipForCapability"
    - from: "src/validation/check-catalog.ts"
      to: "the eight required package-script names"
      via: "one fixed catalog order and one unique check ID per script; Plan 08 supplies production adapters and package wiring in Plan 09 projects from this contract"
      pattern: "CHECK_CATALOG"
    - from: "test:integration descriptor"
      to: "Plan 07 injected runner boundary"
      via: "external-only policy forbids in-suite self-execution and requires a fake/stub when catalog baselines are evaluated internally"
      pattern: "external"
    - from: "fixed-process and capability descriptors"
      to: "Plan 08 production adapters and process entry"
      via: "the catalog supplies the only executable/argv and named capability IDs; the process entry accepts check IDs, never arbitrary commands"
      pattern: "execution"
  edges:
    - "R9 adjacency: eight distinct script identities map to eight distinct descriptors, never aliases or successful no-ops (AC-19)."
    - "R9 empty: absent product implementation is FAIL; SKIP is reserved for named unavailable capability evidence (AC-21)."
    - "R10 adjacency: each normalized check has one mutually exclusive status (AC-22)."
    - "R10 empty: missing, empty, contradictory, and unknown inputs normalize to FAIL (AC-22)."
    - "R10 ordering: catalog and aggregate order are stable for repeated controlled inputs (AC-23)."
  prohibitions:
    - "Validation MUST NOT promote a skip, replay, fixture, mock, unavailable shell, or absent product into a passing live result."
    - "A check descriptor MUST NOT contain a shell string, mutable argument list, recursive package-script call, or implicit successful fallback."
---

## Phase Goal

**As an** operator or maintainer, **I want to** use one collision-safe MLX product surface and inspect an honest validation baseline plus complete replacement map, **so that** I can operate and evolve the brownfield system without ambiguous identity, unsafe takeover, or false acceptance.

<objective>
Establish the pure validation semantics and ordered eight-check contract before any host process or package script is wired.

Purpose: Make truthful status/source/capability classification and non-recursive execution policy impossible to bypass in later runtime and package layers.
Output: Typed result, capability, and check-catalog modules with fail-first unit matrices.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/SKELETON.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-CONTEXT.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-SPEC.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-UI-SPEC.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-RESEARCH.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-PATTERNS.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-VALIDATION.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-05-SUMMARY.md

<interfaces>
- Follow `01-PATTERNS.md` “Validation results, check catalog, runner, and report”: discriminated result states follow `lib/training/supervisor.ts`; the ordered catalog follows the literal registry shape in `src/commands/index.ts` without its dynamic handlers.
- `ValidationResult` retains `{ checkId, status, source, reason, capability? }`; source is never inferred from status.
- `CheckDescriptor` declares one of fixed-process, product-gate, capability-gate, or external-harness execution. It does not execute anything and cannot hold a shell command; fixed/external process variants carry frozen executable/argv tokens consumed by Plan 08.
- `test:integration` uses external-harness execution with the fixed direct Vitest argv so a process already running that suite can inspect or stub the descriptor without spawning itself; only Plan 08's external mode may execute those tokens.
</interfaces>
</context>

## Artifacts this phase produces

- New paths: `src/validation/result.ts`, `src/validation/capabilities.ts`, `src/validation/check-catalog.ts`, and their co-located tests.
- New symbols/types: `ValidationStatus`, `EvidenceSource`, `ValidationResultInput`, `ValidationResult`, `CapabilityEvidence`, `CheckDescriptor`, `CHECK_CATALOG`, `normalizeValidationResult`, `aggregateValidationResults`, `normalizeCapabilityEvidence`, `canSkipForCapability`, and `getCheckDescriptor`.
- Stable script/check identities: `check`, `typecheck`, `test`, `test:integration`, `studio:build`, `dataset:validate`, `benchmark:smoke`, and `local:check`.
- Later-product owner gates: Studio Phase 8, dataset Phase 5, benchmark Phase 6; these are typed LIVE FAIL results until the owning products exist.

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1: RED — Define fail-closed result and capability matrices</name>
  <files>src/validation/result.test.ts, src/validation/capabilities.test.ts</files>
  <read_first>
    - `01-SPEC.md` AC-21 through AC-23 and R10 edges
    - `01-CONTEXT.md` independent status/source and capability-only skip decision
    - `01-UI-SPEC.md` empty, partial, failure, and capability-skip copy
    - `01-PATTERNS.md` result discriminant analog from `lib/training/supervisor.ts`
  </read_first>
  <behavior>
    - Valid rows preserve exactly one status and their declared source.
    - Missing, empty, unknown, contradictory, or structurally invalid rows become visible FAIL results with stable reasons.
    - SKIP requires a named probe with `available: false`; missing implementation or an unprobed capability becomes FAIL.
    - Aggregation retains all rows, synthesizes FAIL for zero rows, and applies FAIL over SKIP over PASS.
  </behavior>
  <acceptance_criteria>
    - Tests cover each invalid shape independently and assert source never promotes status.
    - The Plan 01 gate runs every declared test file, records its exit, and accepts RED only for failed behavior assertions rather than setup/import/collection errors.
  </acceptance_criteria>
  <action>
Write the result, capability, aggregation, zero-row, and ordering tests first. Use the Plan 01 gate to parse Vitest evidence and reject missing-module collection failures as sufficient RED: provide temporary test-local type shims or assertions that collect successfully and fail on missing behavior. Preserve the recorded assertion failures and do not create production modules in this task.
  </action>
  <verify>
    <automated>bun test/support/phase-1-gate.ts expect-red validation-results</automated>
  </verify>
  <done>The RED matrices distinguish every truthful final state, invalid-skip case, source label, empty aggregate, and failure precedence using collected behavior assertions.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2: GREEN — Implement truthful normalization and capability evidence</name>
  <files>src/validation/result.ts, src/validation/capabilities.ts</files>
  <read_first>
    - RED tests from Task 1
    - `01-RESEARCH.md` normalization and aggregate rules
    - `01-PATTERNS.md` typed boundary and expected-failure guidance
  </read_first>
  <behavior>
    - Untrusted checker/capability input is schema-validated before normalization.
    - Expected invalid shapes return structured FAIL rows rather than throwing.
    - Capability evidence remains independent metadata and only authorizes SKIP when named and unavailable.
    - Repeated controlled inputs produce byte-equivalent normalized structures and order.
  </behavior>
  <acceptance_criteria>
    - Task 1 turns GREEN with exhaustive switches and no ad hoc structural casts.
    - No fixture/replay/source value changes the normalized status.
    - No new dependency, timestamp, locale sort, or random identity is introduced.
  </acceptance_criteria>
  <action>
Implement the Zod/typed boundaries, fail-closed normalization, capability eligibility, row retention, and aggregate precedence using the mapped discriminated-union analog. Treat bad checker data as a structured FAIL and reserve throws for internal invariants. This is the TDD GREEN gate for result semantics.
  </action>
  <verify>
    <automated>bun x vitest run src/validation/result.test.ts src/validation/capabilities.test.ts &amp;&amp; bun run typecheck</automated>
  </verify>
  <done>Validation status, source, capability evidence, invalid-input behavior, and aggregate precedence are deterministic and truthful.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3: REFACTOR — Pin the ordered eight-check catalog and recursion policy</name>
  <files>src/validation/check-catalog.test.ts, src/validation/check-catalog.ts</files>
  <read_first>
    - GREEN result/capability interfaces
    - `01-RESEARCH.md` eight-script semantics and recursion warning
    - `01-PATTERNS.md` catalog analog in `src/commands/index.ts`
    - `AGENTS.md` eight stable validation commands
  </read_first>
  <behavior>
    - Exactly eight unique ordered descriptors exist with stable script and check IDs.
    - Fixed-process descriptors contain immutable executable/argument arrays and no shell text: check selects Bun→Biome `check .`, typecheck selects Bun→TypeScript `--noEmit`, and test selects Bun→Vitest with the unit config.
    - Product gates carry exact owner phases and LIVE FAIL semantics; local check carries named capability policy.
    - `test:integration` is explicitly external-harness-only, carries fixed direct Vitest integration-config argv, and exposes a typed stub seam for internal catalog/baseline tests.
  </behavior>
  <acceptance_criteria>
    - Catalog invariants fail before the declaration and pass afterward.
    - No descriptor calls `bun run` for its own script, imports a later product, accepts caller-provided argv, or converts missing implementation to SKIP.
    - All Task 1/2 tests remain green.
  </acceptance_criteria>
  <action>
Define the literal ordered catalog using the mapped registry pattern and pin all uniqueness, owner, source, argument, and execution-policy invariants. Freeze executable/argv tokens for Biome, TypeScript, unit Vitest, and direct integration Vitest; no descriptor may use `bun run`, a shell string, caller-supplied arguments, watch mode, or a path outside the allowlist. Model self-referential integration as an external-harness descriptor that Plan 07 must stub under injected unit/baseline execution and Plan 08 may invoke only through explicit external mode from outside the integration process. Refactor shared literal types only after all catalog tests pass.
  </action>
  <verify>
    <automated>bun x vitest run src/validation/result.test.ts src/validation/capabilities.test.ts src/validation/check-catalog.test.ts &amp;&amp; bun run typecheck</automated>
  </verify>
  <done>The pure validation contract has eight unique honest descriptors and an enforceable non-recursive integration policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Checker/capability input → normalization | Untrusted, partial, contradictory, or missing values attempt to become acceptance. |
| Catalog declaration → later runner | A malformed descriptor could authorize shell input, recursion, or a false product result. |
| Status/source → acceptance claim | Fixture, replay, skip, or unavailable state can be misrepresented as live pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-24 | Tampering | `src/validation/result.ts` | high | mitigate | Schema-parse and normalize missing/unknown/contradictory/invalid-skip input to explicit FAIL rows. |
| T-01-25 | Spoofing | `src/validation/capabilities.ts` | high | mitigate | Require named unavailable probe evidence; source and owner metadata cannot authorize SKIP. |
| T-01-26 | Elevation of Privilege | `src/validation/check-catalog.ts` | high | mitigate | Discriminated descriptor kinds, immutable argv, no shell field, and external-only recursion policy tests. |
| T-01-27 | Repudiation | ordered catalog/result rows | medium | mitigate | Stable IDs, reasons, owners, capabilities, fixed order, and all-row retention. |
</threat_model>

<verification>
- `bun x vitest run src/validation/result.test.ts src/validation/capabilities.test.ts src/validation/check-catalog.test.ts`
- `bun run typecheck`
- Inspect the catalog: eight unique entries, exact immutable Biome/TypeScript/unit-Vitest/direct-integration-Vitest argv, no shell strings or `bun run`, product owners 8/5/6, named local capability, and external-only `test:integration`.
</verification>

<success_criteria>
- Invalid or incomplete result shapes fail closed and remain visible.
- Status and evidence source are independent; a valid SKIP requires named unavailable capability evidence.
- The eight required scripts have one ordered typed descriptor each.
- Product absence is LIVE FAIL, and the integration descriptor cannot recursively run itself.
</success_criteria>

<output>
Create `.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-06-SUMMARY.md` when done.
</output>
