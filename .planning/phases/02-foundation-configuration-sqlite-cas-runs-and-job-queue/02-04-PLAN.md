---
phase: 02-foundation-configuration-sqlite-cas-runs-and-job-queue
plan: "04"
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - migrations/0002-blobs.sql
  - src/storage/atomic-file.ts
  - src/storage/atomic-file.test.ts
  - src/storage/object-store.ts
  - src/storage/object-store.test.ts
  - src/catalog/blob-references.ts
  - src/catalog/blob-references.test.ts
  - test/integration/object-store.test.ts
autonomous: true
requirements: [FNDN-06]
must_haves:
  truths:
    - "Source, patch, trace, report, and generated bytes round-trip at sha256/<prefix>/<digest> and corruption is detected."
    - "Concurrent equal writes converge on one immutable object and catalog references never point to absent bytes."
  artifacts:
    - path: src/storage/object-store.ts
      provides: "Atomic verified SHA-256 put/open/verify boundary"
    - path: src/catalog/blob-references.ts
      provides: "Typed durable object reference API"
  key_links:
    - from: src/catalog/blob-references.ts
      to: src/storage/object-store.ts
      via: "reference transaction verifies committed digest before insert"
---

## Phase Goal

**As an** MLX operator, **I want to** run every workflow on contained configuration, migrated state, immutable artifacts, and recoverable orchestration, **so that** interrupted local work remains private, inspectable, and safe to resume.

<objective>Deliver an end-to-end immutable artifact slice backed by contained SHA-256 bytes and durable references.</objective>
<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@docs/MLX_PROJECT_SPEC.md
@docs/MLX_DATASET_CONTRACT.md
@.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-01-SUMMARY.md
@.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-02-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true" gate="RED">
  <name>Task 1: RED — Specify object round-trip, convergence, and corruption</name>
  <files>test/integration/object-store.test.ts</files>
  <behavior>
    - Every required kind stores bytes once, returns lowercase SHA-256 metadata, reopens identical bytes, and records a typed reference.
    - Interrupted temp writes vanish safely; concurrent equal puts converge; digest/path substitution, truncation, symlinks, and conflicting bytes fail closed.
  </behavior>
  <action>Write tests against a synthetic owned root/catalog with kill-point and concurrent-writer seams. Assert catalog reference insertion cannot precede durable object commit.</action>
  <verify><automated>bun x vitest run test/integration/object-store.test.ts</automated></verify>
  <done>RED names atomicity, integrity, immutability, and reference ordering failures.</done>
</task>
<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2: GREEN — Implement atomic immutable SHA-256 storage</name>
  <files>src/storage/atomic-file.ts, src/storage/atomic-file.test.ts, src/storage/object-store.ts, src/storage/object-store.test.ts</files>
  <action>Stream or buffer through SHA-256, write a same-filesystem exclusive temp with restrictive mode, fsync file, atomically publish without overwriting a conflicting object, fsync the parent directory, then re-hash on verified open. Reject symlinks and non-regular files at every component. Clean only owned temp names after failure.</action>
  <verify><automated>bun x vitest run src/storage/atomic-file.test.ts src/storage/object-store.test.ts</automated></verify>
  <done>Valid bytes survive crashes and concurrent puts; corrupted or substituted bytes are never returned as valid.</done>
</task>
<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3: REFACTOR — Add typed durable blob references</name>
  <files>migrations/0002-blobs.sql, src/catalog/blob-references.ts, src/catalog/blob-references.test.ts, test/integration/object-store.test.ts</files>
  <action>Add immutable digest metadata and typed source/patch/trace/report/generated references through a numbered migration. Insert references only after object verification, enforce digest format/size/kind in schema and TypeScript, and make duplicate equivalent references idempotent while rejecting semantic conflicts.</action>
  <verify><automated>bun x vitest run src/catalog/blob-references.test.ts test/integration/object-store.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Every required artifact kind has verified bytes and a durable typed reference with no dangling commit window.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Untrusted artifact bytes/path state → immutable CAS and catalog | Bytes may collide, corrupt, race, or be replaced through filesystem tricks. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-10 | Tampering | object store | high | mitigate | SHA-256, exclusive same-root temp, fsync, atomic publish, verified reads. |
| T-02-11 | Elevation of Privilege | object path | high | mitigate | Fixed digest-derived paths plus non-following component checks. |
| T-02-12 | Repudiation | blob references | medium | mitigate | Typed kind, size, digest, timestamps, and transactional reference insertion. |
</threat_model>
<source_coverage>FNDN-06, GOAL immutable artifacts, dataset-contract provenance/reference needs, and CONTEXT CAS constraints are COVERED. No research item exists.</source_coverage>
<verification>Run focused tests, `bun run typecheck`, and `bun run check`; manually inspect that test roots, not repository `data/`, receive generated bytes.</verification>
<success_criteria>All required artifact kinds round-trip atomically by digest and every reference resolves to verified bytes.</success_criteria>
<output>Create `.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-04-SUMMARY.md` when done.</output>
