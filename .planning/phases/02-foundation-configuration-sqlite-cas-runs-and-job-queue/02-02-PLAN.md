---
phase: 02-foundation-configuration-sqlite-cas-runs-and-job-queue
plan: "02"
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - migrations/0001-catalog.sql
  - src/catalog/connection.ts
  - src/catalog/connection.test.ts
  - src/catalog/migration-runner.ts
  - src/catalog/migration-runner.test.ts
  - test/integration/catalog-migrations.test.ts
  - package.json
autonomous: true
requirements: [FNDN-03, FNDN-04]
must_haves:
  truths:
    - "Opening a fresh or supported prior catalog applies persisted numbered migrations transactionally and preserves data."
    - "Every opened catalog proves foreign_keys=ON, journal_mode=WAL, and a recorded schema version before use."
  artifacts:
    - path: migrations/0001-catalog.sql
      provides: "Persisted base operational schema and migration ledger"
    - path: src/catalog/migration-runner.ts
      provides: "Ordered checksum-verified transactional migration authority"
    - path: src/catalog/connection.ts
      provides: "Contained Bun SQLite connection boundary"
  key_links:
    - from: src/catalog/connection.ts
      to: migrations/0001-catalog.sql
      via: "open invokes migration runner before returning a usable handle"
---

## Phase Goal

**As an** MLX operator, **I want to** run every workflow on contained configuration, migrated state, immutable artifacts, and recoverable orchestration, **so that** interrupted local work remains private, inspectable, and safe to resume.

<objective>
Make `mlx init` followed by catalog open a real durable SQLite slice with persisted, repeatable upgrades.

Purpose: Give every later subsystem one versioned operational-state authority.
Output: Base SQL migration, Bun SQLite connection/migration modules, and fresh/upgrade acceptance tests.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/MLX_PROJECT_SPEC.md
@.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-01-SUMMARY.md
@src/cli/init.ts
@package.json

Use Bun's built-in `bun:sqlite`; add no package dependency. Inspect and preserve the operator's existing `package.json` diff, changing only packaged-file coverage if execution proves it necessary.
</context>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1: RED — Specify fresh, repeat, and prior-schema upgrades</name>
  <files>test/integration/catalog-migrations.test.ts</files>
  <behavior>
    - Fresh open creates the catalog and ledger, enables WAL and foreign keys, and is idempotent on reopen.
    - A supported prior fixture upgrades without row loss; a changed checksum, gap, unknown future version, or failed statement leaves the prior transaction intact.
  </behavior>
  <action>Write temporary-MLX_HOME integration tests that invoke the public catalog open seam after owned-root initialization. Include crash/failure injection around migration transactions and prove no ad hoc schema mutation.</action>
  <verify><automated>bun x vitest run test/integration/catalog-migrations.test.ts</automated></verify>
  <done>The suite fails on missing migration/catalog behavior, not setup.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2: GREEN — Implement the contained SQLite connection</name>
  <files>migrations/0001-catalog.sql, src/catalog/connection.ts, src/catalog/connection.test.ts</files>
  <action>Create the catalog only beneath the owned contained root, configure busy timeout, foreign keys, and WAL explicitly, and verify each pragma on every returned connection. Define the migration ledger plus stable base tables needed to reference immutable artifacts without prebuilding later domain schemas. Provide close/transaction APIs, never a caller-selected SQL string boundary.</action>
  <verify><automated>bun x vitest run src/catalog/connection.test.ts</automated></verify>
  <done>A usable connection cannot escape the root or return with required SQLite pragmas disabled.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3: REFACTOR — Persist and verify numbered migrations</name>
  <files>src/catalog/migration-runner.ts, src/catalog/migration-runner.test.ts, test/integration/catalog-migrations.test.ts, package.json</files>
  <action>Load only the application-owned ordered migration manifest, hash exact SQL bytes, execute pending files in one immediate transaction each, and record number/name/checksum. Refuse drift, gaps, downgrade, and unsupported future schemas. Keep migration files packaged with the sole `mlx` executable; make a narrow package metadata edit around current operator changes.</action>
  <verify><automated>bun x vitest run src/catalog/migration-runner.test.ts test/integration/catalog-migrations.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Fresh, repeated, and supported prior opens are deterministic and data-preserving; drift fails before mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Packaged SQL/runtime path → catalog file | Application bytes and local state determine durable schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04 | Tampering | migration ledger | high | mitigate | Exact ordered checksums, gaps/future-version refusal, transactional writes. |
| T-02-05 | Denial of Service | concurrent catalog open | medium | mitigate | Busy timeout, immediate migration transactions, idempotent re-open tests. |
| T-02-06 | Elevation of Privilege | catalog path | high | mitigate | Owned-root and Plan 01 containment gate before SQLite open. |
</threat_model>

<source_coverage>
FNDN-03/FNDN-04 and the GOAL's migrated-state clause are COVERED. CONTEXT SQLite, persisted-migration, typed-boundary, and incremental-brownfield constraints are COVERED. No phase research exists.
</source_coverage>

<verification>Run focused unit/integration tests, `bun run typecheck`, `bun run check`, and reopen the same synthetic prior catalog twice.</verification>
<success_criteria>A catalog is usable only after checksum-valid persisted migration with WAL and foreign keys proven active.</success_criteria>
<output>Create `.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-02-SUMMARY.md` when done.</output>
