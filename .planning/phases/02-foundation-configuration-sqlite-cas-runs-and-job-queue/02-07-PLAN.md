---
phase: 02-foundation-configuration-sqlite-cas-runs-and-job-queue
plan: "07"
type: execute
wave: 6
depends_on: [02-06]
files_modified:
  - src/jobs/process-supervisor.ts
  - src/jobs/process-supervisor.test.ts
  - src/jobs/job-worker.ts
  - src/jobs/job-worker.test.ts
  - test/integration/process-lifecycle.test.ts
autonomous: true
requirements: [FNDN-11]
must_haves:
  truths:
    - "Allowlisted argv children are terminated and recorded after success, failure, timeout, cancellation, or simulated worker crash."
    - "A worker does not release or terminally transition a job until child cleanup and bounded output draining finish."
  artifacts:
    - path: src/jobs/process-supervisor.ts
      provides: "Shell-free bounded process-group lifecycle authority"
    - path: src/jobs/job-worker.ts
      provides: "Lease-aware worker wrapper connecting jobs, signals, checkpoints, and cleanup"
  key_links:
    - from: src/jobs/job-worker.ts
      to: src/jobs/process-supervisor.ts
      via: "Abort/timeout/lease loss always enters kill-drain-record-finalize ordering"
---

## Phase Goal

**As an** MLX operator, **I want to** run every workflow on contained configuration, migrated state, immutable artifacts, and recoverable orchestration, **so that** interrupted local work remains private, inspectable, and safe to resume.

<objective>Close the job lifecycle across the host-process boundary without shell injection or orphan work.</objective>
<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@docs/MLX_BENCHMARK_SPEC.md
@.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-06-SUMMARY.md
@src/validation/process-adapter.ts
@lib/server/processes.ts
@src/lib/server-manager.ts

Reuse Phase 1's exact-token, shell-disabled, bounded-stream patterns. The current working tree contains operator edits in subprocess-related files; do not modify those files in this plan.
</context>

<tasks>
<task type="auto" tdd="true" gate="RED">
  <name>Task 1: RED — Specify all child termination paths</name>
  <files>test/integration/process-lifecycle.test.ts</files>
  <behavior>
    - Success, nonzero exit, timeout, AbortSignal cancellation, lease loss, and worker interruption each leave no live descendant and emit a structured terminal event.
    - stdout/stderr bounds are independent; argv is exact tokens; shell/metacharacters are never interpreted.
  </behavior>
  <action>Build a fixed synthetic child fixture using current test process entry points and PID/process-group probes. Exercise every path with bounded deadlines and cleanup in test finally blocks; never run repository-selected commands.</action>
  <verify><automated>bun x vitest run test/integration/process-lifecycle.test.ts</automated></verify>
  <done>RED proves cleanup ordering and orphan detection across every lifecycle outcome.</done>
</task>
<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2: GREEN — Implement shell-free process supervision</name>
  <files>src/jobs/process-supervisor.ts, src/jobs/process-supervisor.test.ts</files>
  <action>Accept only immutable allowlist descriptors, copy argv, spawn without a shell, register pid/process group before work, bound both streams, and on timeout/abort escalate TERM then KILL with bounded waits. Always drain/await exit, record reason/code/signal/timing/truncation, and unregister in finally. Keep platform-specific group handling capability-reported.</action>
  <verify><automated>bun x vitest run src/jobs/process-supervisor.test.ts</automated></verify>
  <done>The supervisor has one deterministic cleanup path and cannot accept command strings.</done>
</task>
<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3: REFACTOR — Bind supervision to fenced job execution</name>
  <files>src/jobs/job-worker.ts, src/jobs/job-worker.test.ts, test/integration/process-lifecycle.test.ts</files>
  <action>Make the worker acquire/heartbeat/checkpoint through its fencing token, translate cancellation and lease loss to AbortSignal, wait for supervisor cleanup, append the process outcome, then retry or terminally transition. On startup, reconcile recorded live children from interrupted workers before reclaiming their jobs; never signal an unverified reused PID.</action>
  <verify><automated>bun x vitest run src/jobs/job-worker.test.ts test/integration/process-lifecycle.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Job state and child state converge after every outcome with no orphan or stale-worker completion.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Typed job descriptor → host process table | Process creation can inject commands, leak output, survive cancellation, or target reused PIDs. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-20 | Elevation of Privilege | process spawn | high | mitigate | Exact immutable allowlist, token argv, shell disabled. |
| T-02-21 | Denial of Service | child lifecycle | high | mitigate | Bounded streams/timeouts, TERM→KILL, await exit, startup reconciliation. |
| T-02-22 | Spoofing | orphan PID reconciliation | high | mitigate | Verify recorded process identity/start evidence before signaling. |
| T-02-23 | Information Disclosure | process output | medium | mitigate | Independent bounds and structured sanitized event storage. |
</threat_model>
<source_coverage>FNDN-11, GOAL crash recovery, benchmark-spec orphan cleanup, AGENTS deny-by-default shell rule, and CONTEXT subprocess/AbortSignal pattern are COVERED. No research item exists.</source_coverage>
<verification>Run focused tests, `bun run typecheck`, `bun run check`, and inspect the test process table after each lifecycle case.</verification>
<success_criteria>No allowed lifecycle path leaves a child alive or permits caller-selected shell execution, and every outcome is durably recorded.</success_criteria>
<output>Create `.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-07-SUMMARY.md` when done.</output>
