---
phase: 02-foundation-configuration-sqlite-cas-runs-and-job-queue
plan: "09"
type: execute
wave: 6
depends_on: [02-08]
files_modified:
  - src/cli/gc.ts
  - src/cli/gc.test.ts
  - src/cli/main.ts
  - src/cli/main.test.ts
  - src/cli/command-tree.ts
  - src/cli/command-tree.test.ts
  - test/integration/gc-command.test.ts
autonomous: true
requirements: [FNDN-12]
must_haves:
  truths:
    - "mlx gc is an implemented owner-Phase-2 leaf with deterministic human and JSON usage/dry-run output."
    - "Only explicit --apply can request collection, and incomplete ownership/reference evidence fails without deletion."
  artifacts:
    - path: src/cli/gc.ts
      provides: "Thin fixed-policy handler for usage, dry-run, and explicit apply"
    - path: test/integration/gc-command.test.ts
      provides: "Process-level public command and non-destructive-default proof"
  key_links:
    - from: src/cli/gc.ts
      to: src/storage/gc.ts
      via: "handler selects fixed dry-run/apply mode and returns the structured service result"
---

## Phase Goal

**As an** MLX operator, **I want to** run every workflow on contained configuration, migrated state, immutable artifacts, and recoverable orchestration, **so that** interrupted local work remains private, inspectable, and safe to resume.

<objective>Expose Plan 08's proven retention service through the canonical `mlx gc` command without weakening the Phase 1 CLI boundary.</objective>
<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-08-SUMMARY.md
@src/cli/command-tree.ts
@src/cli/main.ts

Inspect current diffs in shared CLI files before editing. Preserve the literal catalog as authority, make only `gc` newly available, retain one final write in the executable entry, and do not disturb unrelated operator hunks.
</context>

<tasks>
<task type="auto" tdd="true" gate="RED">
  <name>Task 1: RED — Specify the public gc command contract</name>
  <files>src/cli/gc.test.ts, test/integration/gc-command.test.ts</files>
  <behavior>
    - Default human/JSON invocation reports usage, candidate/protected counts, errors, and zero deletions.
    - `--apply` is the sole destructive request; invalid extra options, unowned roots, owner conflicts, and incomplete scans are nonzero and non-destructive.
  </behavior>
  <action>Write injected handler tests plus a process-level isolated-MLX_HOME test. Assert no prompt, arbitrary root, force option, second executable, repository data read, or output outside the existing one-document renderer.</action>
  <verify><automated>bun x vitest run src/cli/gc.test.ts test/integration/gc-command.test.ts</automated></verify>
  <done>RED covers public reachability and explicit destructive intent.</done>
</task>
<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2: GREEN — Activate only the canonical gc leaf</name>
  <files>src/cli/gc.ts, src/cli/main.ts, src/cli/command-tree.ts</files>
  <action>Add the fixed `--apply` flag to the existing owner-Phase-2 leaf, mark only that leaf implemented, and inject a thin handler through `runCli`. Resolve the owned root/catalog through established dependencies, call Plan 08 with dry-run unless the flag is present, and return a stable envelope without importing the brownfield UI graph.</action>
  <verify><automated>bun x vitest run src/cli/gc.test.ts test/integration/gc-command.test.ts</automated></verify>
  <done>`mlx gc` reaches the proven service and remains non-destructive by default.</done>
</task>
<task type="auto" gate="REFACTOR">
  <name>Task 3: REFACTOR — Preserve CLI catalog and rendering invariants</name>
  <files>src/cli/main.test.ts, src/cli/command-tree.test.ts, test/integration/gc-command.test.ts</files>
  <action>Update exact catalog/orchestration snapshots for the one Phase 2 availability change and apply option. Prove all other later-phase leaves remain unavailable before handler lookup, parse/help stay catalog-derived, human and JSON results contain complete collection evidence, and the executable still performs one final write.</action>
  <verify><automated>bun x vitest run src/cli/gc.test.ts src/cli/main.test.ts src/cli/command-tree.test.ts test/integration/gc-command.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>The public GC slice is reachable with no CLI identity, availability, or output regression.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Operator argv → destructive retention service | Parsing or handler drift could apply deletion without explicit intent or to the wrong root. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-28 | Elevation of Privilege | gc argv/root | high | mitigate | Literal catalog option, owned canonical root only, no arbitrary path or force flag. |
| T-02-29 | Tampering | apply selection | high | mitigate | Default dry-run and exact boolean `--apply` mapping to the fixed service mode. |
| T-02-30 | Repudiation | CLI result | medium | mitigate | Stable human/JSON candidate, protected, deleted, and error evidence. |
</threat_model>
<source_coverage>FNDN-12 public reachability and the GOAL's operator retention criterion are COVERED. Across Plans 01-09, every FNDN-01..FNDN-12 requirement, every ROADMAP Phase 2 success criterion, and every non-deferred CONTEXT constraint is COVERED. RESEARCH.md is absent by configured policy, so no research feature is unplanned.</source_coverage>
<verification>Run focused tests, `bun run typecheck`, `bun run check`, then execute default and `--apply` modes against an isolated synthetic root.</verification>
<success_criteria>Operator can inspect usage and explicitly reclaim only proven unreachable objects through the sole `mlx` executable.</success_criteria>
<output>Create `.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-09-SUMMARY.md` when done.</output>
