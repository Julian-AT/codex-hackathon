---
phase: 02-foundation-configuration-sqlite-cas-runs-and-job-queue
plan: "05"
type: execute
wave: 4
depends_on: [02-02, 02-04]
files_modified:
  - migrations/0003-runs.sql
  - src/runs/run-manifest.ts
  - src/runs/run-manifest.test.ts
  - src/runs/stage-reuse.ts
  - src/runs/stage-reuse.test.ts
  - test/integration/run-reuse.test.ts
autonomous: true
requirements: [FNDN-07, FNDN-08]
must_haves:
  truths:
    - "Every run commits an immutable checksummed manifest with inputs, config, implementation, lineage, outputs, and object checksums."
    - "Identical deterministic stage inputs reuse only a completed, schema-valid, checksum-valid output; invalid or incomplete work executes again."
  artifacts:
    - path: src/runs/run-manifest.ts
      provides: "Canonical versioned run manifest builder/committer/verifier"
    - path: src/runs/stage-reuse.ts
      provides: "Idempotency fingerprint claim and verified reuse decision"
  key_links:
    - from: src/runs/stage-reuse.ts
      to: src/runs/run-manifest.ts
      via: "reuse requires verified committed output manifest and CAS objects"
---

## Phase Goal

**As an** MLX operator, **I want to** run every workflow on contained configuration, migrated state, immutable artifacts, and recoverable orchestration, **so that** interrupted local work remains private, inspectable, and safe to resume.

<objective>Make deterministic work visible as checksummed run lineage and safely reusable after completion.</objective>
<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@docs/MLX_PROJECT_SPEC.md
@docs/MLX_DATASET_CONTRACT.md
@.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-04-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true" gate="RED">
  <name>Task 1: RED — Specify run lineage and deterministic reuse</name>
  <files>test/integration/run-reuse.test.ts</files>
  <behavior>
    - A child run commits canonical manifest bytes and the parent identifies it by digest.
    - Repeated equal stage input invokes the side effect once; changed config/version/input invokes it again.
    - Running, failed, cancelled, corrupt, missing-object, or checksum-mismatched candidates are never reused.
  </behavior>
  <action>Write an integration harness with a counted fake side effect, crash points before/after manifest publication, and synthetic CAS/catalog. Assert timestamps and run IDs are excluded from the deterministic stage key but retained in lineage.</action>
  <verify><automated>bun x vitest run test/integration/run-reuse.test.ts</automated></verify>
  <done>The RED suite distinguishes safe reuse from accidental duplicate suppression.</done>
</task>
<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2: GREEN — Commit canonical immutable run manifests</name>
  <files>migrations/0003-runs.sql, src/runs/run-manifest.ts, src/runs/run-manifest.test.ts</files>
  <action>Define strict versioned schemas and code-point canonical JSON for run/stage IDs, input/config/implementation hashes, parent/child lineage, outputs, validation results, and checksums. Store manifest bytes through CAS, atomically record the committed digest/status in SQLite, and prohibit mutation after terminal commit; append events instead.</action>
  <verify><automated>bun x vitest run src/runs/run-manifest.test.ts</automated></verify>
  <done>Committed run lineage is byte-stable, checksummed, CAS-backed, and immutable.</done>
</task>
<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3: REFACTOR — Claim and reuse verified deterministic stages</name>
  <files>src/runs/stage-reuse.ts, src/runs/stage-reuse.test.ts, test/integration/run-reuse.test.ts</files>
  <action>Hash a domain-separated canonical tuple of stage type, validated inputs/config, implementation version, and schema version. Use a unique catalog claim so concurrent callers elect one producer. Return reuse only after terminal status, manifest checksum, every output object, and expected validation are verified; otherwise mark invalid evidence and allow recovery execution.</action>
  <verify><automated>bun x vitest run src/runs/stage-reuse.test.ts test/integration/run-reuse.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Deterministic equal work is performed once and reused only from intact committed evidence.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Stage inputs/implementation → reuse cache | Crafted or stale identity can alias distinct work or reuse corrupt outputs. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-13 | Spoofing | idempotency key | high | mitigate | Domain-separated canonical hashes include stage/config/input/implementation/schema. |
| T-02-14 | Tampering | run manifest | high | mitigate | Canonical bytes in CAS plus catalog digest and full output verification. |
| T-02-15 | Repudiation | run lineage | medium | mitigate | Immutable parent/child IDs, versions, checksums, results, and append-only events. |
</threat_model>
<source_coverage>FNDN-07/FNDN-08, GOAL lineage/reuse, project-spec reproducibility, and CONTEXT manifest/idempotency constraints are COVERED. No research item exists.</source_coverage>
<verification>Run focused tests, `bun run typecheck`, `bun run check`, and repeat the integration harness to prove the side-effect count remains one.</verification>
<success_criteria>Every terminal run has verified immutable lineage and repeated equal deterministic stages reuse intact committed outputs only.</success_criteria>
<output>Create `.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-05-SUMMARY.md` when done.</output>
