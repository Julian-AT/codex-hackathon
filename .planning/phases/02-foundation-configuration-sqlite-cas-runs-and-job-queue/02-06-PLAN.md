---
phase: 02-foundation-configuration-sqlite-cas-runs-and-job-queue
plan: "06"
type: execute
wave: 5
depends_on: [02-02, 02-05]
files_modified:
  - migrations/0004-jobs.sql
  - src/jobs/job-queue.ts
  - src/jobs/job-queue.test.ts
  - src/jobs/job-events.ts
  - src/jobs/job-events.test.ts
  - test/integration/job-recovery.test.ts
autonomous: true
requirements: [FNDN-09, FNDN-10]
must_haves:
  truths:
    - "Durable leased jobs fence stale workers, heartbeat, retry within bounds, checkpoint, cancel, and recover expired leases."
    - "After simulated termination, an operator-visible status projection resumes from checkpoint without duplicate committed outputs and retains every attempt/failure event."
  artifacts:
    - path: src/jobs/job-queue.ts
      provides: "Transactional enqueue/claim/heartbeat/checkpoint/cancel/recover state machine"
    - path: src/jobs/job-events.ts
      provides: "Structured status, progress, attempt, and failure projection"
  key_links:
    - from: src/jobs/job-queue.ts
      to: src/runs/stage-reuse.ts
      via: "job completion commits/reuses one fenced stage output before terminal transition"
---

## Phase Goal

**As an** MLX operator, **I want to** run every workflow on contained configuration, migrated state, immutable artifacts, and recoverable orchestration, **so that** interrupted local work remains private, inspectable, and safe to resume.

<objective>Deliver inspectable, cancellable, crash-recoverable durable work with exactly-once committed outputs.</objective>
<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@docs/MLX_PROJECT_SPEC.md
@.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-05-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true" gate="RED">
  <name>Task 1: RED — Specify leases, fencing, cancellation, and crash resume</name>
  <files>test/integration/job-recovery.test.ts</files>
  <behavior>
    - One queued job has one active lease; stale tokens cannot heartbeat, checkpoint, emit progress, commit, retry, or complete.
    - Expiry/crash produces a recovery event and resumes from the last durable checkpoint within bounded attempts.
    - Cancellation is idempotent, visible, and prevents new output commit; repeated recovery never duplicates a committed output.
  </behavior>
  <action>Use an injected clock/worker identity and abrupt close/reopen points against a temporary catalog. Assert complete ordered status/progress/attempt/failure events and no dependence on wall-clock sleeps.</action>
  <verify><automated>bun x vitest run test/integration/job-recovery.test.ts</automated></verify>
  <done>RED covers the complete durable lifecycle and operator inspection contract.</done>
</task>
<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2: GREEN — Implement the fenced leased-job state machine</name>
  <files>migrations/0004-jobs.sql, src/jobs/job-queue.ts, src/jobs/job-queue.test.ts</files>
  <action>Add jobs, attempts, leases, checkpoints, dependencies, and cancellation through one migration. Implement transactional compare-and-swap transitions with monotonic fencing tokens, explicit lease owner/expiry, bounded retry policy, idempotency key, parent/child lineage, and stale-lease recovery. Validate payload/checkpoint schemas at the boundary and use injected time.</action>
  <verify><automated>bun x vitest run src/jobs/job-queue.test.ts</automated></verify>
  <done>Only the current fenced lease can mutate a running job and every terminal/retry transition is durable.</done>
</task>
<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3: REFACTOR — Project structured status and resume without duplication</name>
  <files>src/jobs/job-events.ts, src/jobs/job-events.test.ts, test/integration/job-recovery.test.ts</files>
  <action>Persist append-only structured lifecycle/progress/failure events with stable codes and sanitized bounded messages. Expose typed list/get/cancel/resume operations for later thin CLI handlers. On resume, combine checkpoint state with Plan 05's committed-output verification so a post-commit crash completes without repeating side effects.</action>
  <verify><automated>bun x vitest run src/jobs/job-events.test.ts test/integration/job-recovery.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Operator-facing status reconstructs the full lifecycle and crash resume produces one valid output.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Competing workers/expired leases → durable job state | Stale workers may overwrite checkpoints or duplicate side effects. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-16 | Elevation of Privilege | lease mutation | high | mitigate | Transactional owner plus monotonic fencing-token predicates on every mutation. |
| T-02-17 | Denial of Service | retry/recovery | high | mitigate | Bounded attempts/backoff, cancellation, expiry recovery, no sleep-based tests. |
| T-02-18 | Repudiation | job events | medium | mitigate | Append-only ordered attempt/progress/failure/cancellation records. |
| T-02-19 | Tampering | checkpoint/output boundary | high | mitigate | Typed checkpoint validation and committed-output verification before terminal status. |
</threat_model>
<source_coverage>FNDN-09/FNDN-10, GOAL recoverable orchestration, project-spec job model, and CONTEXT resumability/observability requirements are COVERED. No research item exists.</source_coverage>
<verification>Run focused tests, `bun run typecheck`, and `bun run check`; repeat crash points before and after output commit.</verification>
<success_criteria>Jobs survive catalog reopen, stale workers are fenced, cancellation/retry are bounded, and resume never duplicates committed outputs.</success_criteria>
<output>Create `.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-06-SUMMARY.md` when done.</output>
