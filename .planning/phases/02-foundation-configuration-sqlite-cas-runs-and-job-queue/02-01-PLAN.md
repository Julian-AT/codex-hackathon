---
phase: 02-foundation-configuration-sqlite-cas-runs-and-job-queue
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/contained-path.ts
  - src/core/contained-path.test.ts
  - src/core/archive-path.ts
  - src/core/archive-path.test.ts
  - src/lib/config.ts
  - src/lib/config.test.ts
  - test/integration/foundation-config.test.ts
autonomous: true
requirements: [FNDN-01, FNDN-02]
must_haves:
  truths:
    - "An operator gets field-specific diagnostics for malformed production configuration."
    - "Every mutable path is derived from the owned MLX_HOME root; lexical traversal, existing symlink escape, and unsafe archive entries fail closed."
  artifacts:
    - path: src/core/contained-path.ts
      provides: "Single non-following containment authority for state paths"
    - path: src/core/archive-path.ts
      provides: "Archive member validation without extraction side effects"
    - path: src/lib/config.ts
      provides: "Strict typed configuration whose path fields resolve through containment"
  key_links:
    - from: src/lib/config.ts
      to: src/core/contained-path.ts
      via: "all configured mutable paths are resolved beneath canonical MLX_HOME"
---

## Phase Goal

**As an** MLX operator, **I want to** run every workflow on contained configuration, migrated state, immutable artifacts, and recoverable orchestration, **so that** interrupted local work remains private, inspectable, and safe to resume.

<objective>
Deliver the first operator-visible foundation slice: loading configuration either yields a complete contained runtime layout or an actionable fail-closed diagnostic.

Purpose: Prevent later phases from inventing paths or crossing the owned state boundary.
Output: Typed configuration, reusable containment/archive guards, and an end-to-end hostile-path contract.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/MLX_PROJECT_SPEC.md
@.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-CONTEXT.md
@.planning/phases/01-identity-cleanup-baseline-and-migration-map/01-03-SUMMARY.md
@src/core/mlx-home.ts
@src/core/state-ownership.ts
@src/lib/config.ts

Before edits, inspect `git status --short` and `git diff --` for every listed existing file; preserve unrelated operator hunks and add only Phase 2-owned changes.
</context>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1: RED — Specify the contained production layout</name>
  <files>test/integration/foundation-config.test.ts</files>
  <behavior>
    - Valid defaults and an absolute override resolve config, catalog, objects, runs, logs, mirrors, worktrees, datasets, models, cache, and parquet beneath one canonical root.
    - Unknown keys, wrong types, traversal segments, symlink ancestors/final targets, and absolute path injection report the exact rejected field/path and make no write.
    - Archive members reject absolute, parent, NUL, drive/UNC, and link escapes before extraction.
  </behavior>
  <action>Write a synthetic-root end-to-end contract first. Use injected filesystem seams and late module loading so RED is a named behavior failure rather than an import or setup failure. Do not inspect a real home directory.</action>
  <verify><automated>bun x vitest run test/integration/foundation-config.test.ts</automated></verify>
  <done>The suite collects and fails only on missing containment/configuration behavior.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2: GREEN — Implement the path and archive authorities</name>
  <files>src/core/contained-path.ts, src/core/contained-path.test.ts, src/core/archive-path.ts, src/core/archive-path.test.ts</files>
  <action>Implement pure lexical normalization followed by component-wise lstat/realpath checks against the canonical root. Permit a missing suffix only after every existing ancestor is proven inside the non-symlink root. Validate archive member names and declared link targets independently; expose no generic extractor or write primitive. Return typed diagnostics instead of silently normalizing hostile input.</action>
  <verify><automated>bun x vitest run src/core/contained-path.test.ts src/core/archive-path.test.ts</automated></verify>
  <done>Hostile traversal, symlink, and archive cases fail without writes; valid contained paths are stable.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3: REFACTOR — Route production configuration through containment</name>
  <files>src/lib/config.ts, src/lib/config.test.ts, test/integration/foundation-config.test.ts</files>
  <action>Make the Zod boundary strict, add a typed immutable runtime-path projection, keep legacy callable exports only where Phase 1 requires them, and resolve every mutable/extracted destination through the new authority. Preserve config at MLX_HOME/config/config.json and use atomic replacement for writes; never regain project-local precedence. Finish the hostile-root integration matrix.</action>
  <verify><automated>bun x vitest run src/lib/config.test.ts test/integration/foundation-config.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Operators receive actionable typed configuration and no accepted path can escape MLX_HOME.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Environment/config/archive metadata → local filesystem | Untrusted strings select destinations under private operator state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Elevation of Privilege | contained-path resolver | high | mitigate | Component-wise non-following containment and missing-suffix rules. |
| T-02-02 | Tampering | archive member validator | high | mitigate | Reject absolute, traversal, NUL, drive/UNC, and link escapes before writes. |
| T-02-03 | Information Disclosure | config diagnostics | medium | mitigate | Report fields and safe paths, never secret values or file contents. |
</threat_model>

<source_coverage>
GOAL containment and FNDN-01/FNDN-02 are COVERED here. CONTEXT has no numbered locked decisions; its contained-state, typed-schema, thin-module, and dirty-worktree constraints are implemented. RESEARCH.md is absent by configuration, so no research item is missing.
</source_coverage>

<verification>
Run `bun x vitest run src/core/contained-path.test.ts src/core/archive-path.test.ts src/lib/config.test.ts test/integration/foundation-config.test.ts`, `bun run typecheck`, and `bun run check`.
</verification>

<success_criteria>
Invalid production config and every tested escape fail closed with zero out-of-root writes; a valid config yields one typed contained runtime layout.
</success_criteria>

<output>Create `.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-01-SUMMARY.md` when done.</output>
