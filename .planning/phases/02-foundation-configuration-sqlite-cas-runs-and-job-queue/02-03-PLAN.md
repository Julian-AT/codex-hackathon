---
phase: 02-foundation-configuration-sqlite-cas-runs-and-job-queue
plan: "03"
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/catalog/catalog-owner.ts
  - src/catalog/catalog-owner.test.ts
  - src/core/sqlite-capability.ts
  - src/core/sqlite-capability.test.ts
  - src/cli/doctor.ts
  - src/cli/doctor.test.ts
autonomous: true
requirements: [FNDN-05]
must_haves:
  truths:
    - "mlx doctor reports the real embedded SQLite version and an independently probed concurrency result."
    - "Catalog mutation remains single-owner unless the WAL-reset race suite proves the current runtime safe."
  artifacts:
    - path: src/catalog/catalog-owner.ts
      provides: "Exclusive renewable catalog-owner gate"
    - path: src/core/sqlite-capability.ts
      provides: "Version plus WAL concurrency capability evidence"
  key_links:
    - from: src/cli/doctor.ts
      to: src/core/sqlite-capability.ts
      via: "doctor includes probed SQLite evidence without weakening executable collision handling"
---

## Phase Goal

**As an** MLX operator, **I want to** run every workflow on contained configuration, migrated state, immutable artifacts, and recoverable orchestration, **so that** interrupted local work remains private, inspectable, and safe to resume.

<objective>Expose honest SQLite capability in `mlx doctor` and enforce the catalog-owner safety gate.</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-02-SUMMARY.md
@src/cli/doctor.ts
@src/core/doctor.ts

Preserve the Phase 1 executable collision classification as the primary doctor outcome; SQLite evidence is additive and cannot turn a collision into success.
</context>

<tasks>
<task type="auto" tdd="true" gate="RED">
  <name>Task 1: RED — Specify doctor capability and owner exclusion</name>
  <files>src/cli/doctor.test.ts</files>
  <behavior>
    - Human and JSON doctor data include embedded version, WAL/foreign-key result, concurrency probe status, and whether multi-owner mutation is allowed.
    - Two contenders cannot both own the catalog; stale or malformed ownership is recovered only by the declared protocol.
  </behavior>
  <action>Extend existing injected doctor tests first, including repeated and concurrent temporary-catalog runs. Assert collision behavior remains fail-closed and no capability claim is inferred from a version string alone.</action>
  <verify><automated>bun x vitest run src/cli/doctor.test.ts</automated></verify>
  <done>Named RED assertions cover the State blocker and FNDN-05.</done>
</task>
<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2: GREEN — Implement the capability probe and catalog owner</name>
  <files>src/core/sqlite-capability.ts, src/core/sqlite-capability.test.ts, src/catalog/catalog-owner.ts, src/catalog/catalog-owner.test.ts</files>
  <action>Read the embedded version from the actual connection and run an isolated, bounded WAL-reset/concurrency fixture. Implement exclusive owner acquisition with nonce, pid/start identity, expiry, atomic compare/release, and fail-closed malformed evidence. Keep single-owner enforcement enabled until this exact probe passes; version thresholds alone never disable it.</action>
  <verify><automated>bun x vitest run src/core/sqlite-capability.test.ts src/catalog/catalog-owner.test.ts</automated></verify>
  <done>Concurrent mutation authority is unique and capability evidence is reproducible.</done>
</task>
<task type="auto" gate="REFACTOR">
  <name>Task 3: REFACTOR — Add SQLite evidence to doctor</name>
  <files>src/cli/doctor.ts, src/cli/doctor.test.ts</files>
  <action>Compose capability evidence with the existing executable inspection through an injected port. Sanitize errors, retain deterministic JSON shape, and explain the operator action when the owner gate blocks mutation. Do not write to a real catalog during ordinary doctor runs; use an isolated capability database under the owned state root.</action>
  <verify><automated>bun x vitest run src/cli/doctor.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>Doctor truthfully reports SQLite and gate status while preserving all Phase 1 collision guarantees.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Concurrent processes → one catalog | Multiple local processes may claim mutation authority or reuse stale evidence. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-07 | Spoofing | catalog owner record | high | mitigate | Exclusive creation, nonce plus process identity, expiry, compare-before-release. |
| T-02-08 | Tampering | SQLite capability evidence | high | mitigate | Execute the isolated race probe; do not trust caller/version assertions. |
| T-02-09 | Information Disclosure | doctor output | low | accept | Only version/status/sanitized paths are shown; no catalog rows or config secrets. |
</threat_model>
<source_coverage>FNDN-05 and the explicit STATE blocker are COVERED. GOAL and CONTEXT doctor/catalog integration constraints are COVERED; no research item exists.</source_coverage>
<verification>Run focused tests plus `bun run typecheck`, `bun run check`, and `bun src/cli.tsx doctor --json` against an isolated owned MLX_HOME.</verification>
<success_criteria>Doctor supplies real SQLite evidence and no two unsafe contenders can mutate the catalog concurrently.</success_criteria>
<output>Create `.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-03-SUMMARY.md` when done.</output>
