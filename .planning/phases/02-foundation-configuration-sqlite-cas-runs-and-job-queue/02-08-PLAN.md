---
phase: 02-foundation-configuration-sqlite-cas-runs-and-job-queue
plan: "08"
type: execute
wave: 5
depends_on: [02-02, 02-04, 02-05]
files_modified:
  - src/storage/usage.ts
  - src/storage/usage.test.ts
  - src/storage/gc.ts
  - src/storage/gc.test.ts
  - test/integration/gc-storage.test.ts
autonomous: true
requirements: [FNDN-12]
must_haves:
  truths:
    - "The GC service defaults to an inspectable dry run and reports disk usage by contained category."
    - "Confirmed collection removes only unreferenced verified objects after a stable mark/recheck window; catalog rows and immutable manifests protect every reachable digest."
  artifacts:
    - path: src/storage/gc.ts
      provides: "Reference-complete mark/recheck/sweep engine"
  key_links:
    - from: src/storage/gc.ts
      to: src/runs/run-manifest.ts
      via: "live-set discovery verifies catalog references and recursively checksummed manifests"
---

## Phase Goal

**As an** MLX operator, **I want to** run every workflow on contained configuration, migrated state, immutable artifacts, and recoverable orchestration, **so that** interrupted local work remains private, inspectable, and safe to resume.

<objective>Build the reference-complete usage and collection service that can report and reclaim only proven unreachable state.</objective>
<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@docs/MLX_PROJECT_SPEC.md
@.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-05-SUMMARY.md
Plan 09 exposes this service through the existing `gc` command after the destructive core is independently proven.
</context>

<tasks>
<task type="auto" tdd="true" gate="RED">
  <name>Task 1: RED — Specify usage, dry-run, apply, and reference safety</name>
  <files>test/integration/gc-storage.test.ts</files>
  <behavior>
    - Human/JSON dry run lists category bytes, candidate counts/digests, protected counts, and zero deletions.
    - Explicit apply removes an old unreferenced object but preserves every digest referenced by catalog rows, manifests, runs, jobs, or a concurrent new reference.
    - Symlinks, corrupt paths, unknown files, active temp writes, and out-of-root candidates fail or remain quarantined rather than being followed/deleted.
  </behavior>
  <action>Write service-level tests using an isolated owned MLX_HOME and injected clock/grace window. Include a mark-to-sweep concurrent-reference hook and prove the default service mode is non-destructive.</action>
  <verify><automated>bun x vitest run test/integration/gc-storage.test.ts</automated></verify>
  <done>RED covers usage reporting and every reference-safety invariant.</done>
</task>
<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2: GREEN — Implement usage and mark/recheck/sweep</name>
  <files>src/storage/usage.ts, src/storage/usage.test.ts, src/storage/gc.ts, src/storage/gc.test.ts</files>
  <action>Walk only fixed contained MLX state categories without following links, aggregate logical/on-disk bytes, and enumerate CAS candidates by strict digest layout. Build the live set from every catalog reference plus recursively verified immutable manifests. Under catalog-owner authority, mark old unreferenced candidates, re-read references after a grace/generation boundary, then unlink only unchanged regular files and fsync parents. Dry run performs no mutation.</action>
  <verify><automated>bun x vitest run src/storage/usage.test.ts src/storage/gc.test.ts</automated></verify>
  <done>Usage is deterministic and sweep cannot delete a referenced, new, changed, linked, or malformed object.</done>
</task>
<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3: REFACTOR — Prove report stability and interrupted sweep recovery</name>
  <files>src/storage/usage.test.ts, src/storage/gc.test.ts, test/integration/gc-storage.test.ts</files>
  <action>Complete the stable structured dry-run/apply projection and kill-point matrix. Reopen after interruption, verify already-deleted candidates idempotently, preserve newly referenced or changed candidates, and require incomplete reference scans to return a non-destructive failure result suitable for the thin CLI in Plan 09.</action>
  <verify><automated>bun x vitest run src/storage/usage.test.ts src/storage/gc.test.ts test/integration/gc-storage.test.ts &amp;&amp; bun run typecheck</automated></verify>
  <done>The storage service is deterministic, resumable, and refuses destructive work on incomplete evidence.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Catalog/manifests/filesystem → destructive deletion | Incomplete or racing reference evidence could authorize data loss or escape the root. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-24 | Tampering | GC live set | high | mitigate | Verify all reference sources and manifests; fail closed on incomplete/corrupt evidence. |
| T-02-25 | Denial of Service | mark/sweep race | high | mitigate | Catalog-owner gate, grace/generation boundary, reference recheck immediately before unlink. |
| T-02-26 | Elevation of Privilege | filesystem sweep | high | mitigate | Fixed contained roots, strict digest paths, lstat regular files, no link traversal. |
| T-02-27 | Repudiation | gc command | medium | mitigate | Stable dry-run/apply report with candidate/protected/deleted/error counts and digests. |
</threat_model>
<source_coverage>FNDN-12 storage safety and GOAL retention safety are COVERED; Plan 09 supplies the required public `mlx gc` reachability. CONTEXT safe-retention and contained-state constraints are COVERED. RESEARCH is absent by configuration.</source_coverage>
<verification>Run focused tests, `bun run typecheck`, `bun run check`, then run dry-run and explicit apply against an isolated synthetic root and compare protected digests before/after.</verification>
<success_criteria>The service reports usage and reclaims proven unreachable objects without deleting anything referenced or crossing MLX_HOME.</success_criteria>
<output>Create `.planning/phases/02-foundation-configuration-sqlite-cas-runs-and-job-queue/02-08-SUMMARY.md` when done.</output>
