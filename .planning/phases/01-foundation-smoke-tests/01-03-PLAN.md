---
phase: 01-foundation-smoke-tests
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - ios/mlx-swift-examples
  - ios/SpecialistApp.entitlements
  - ios/throughput-smoke.log
autonomous: false
requirements: [PLAT-01, PLAT-07]

must_haves:
  truths:
    - "LLMEval fork is cloned into ios/ and building against physical iPhone 17"
    - "modelConfiguration points at the model id written in phases/.../base-model.txt"
    - "com.apple.developer.kernel.increased-memory-limit entitlement is signed into the built app"
    - "App launches on iPhone 17 in airplane mode and generates a response at ≥40 tok/s"
  artifacts:
    - path: "ios/mlx-swift-examples"
      provides: "Forked LLMEval Xcode project with Gemma 4 model plumbing"
      contains: "LLMEval"
    - path: "ios/SpecialistApp.entitlements"
      provides: "Increased memory limit entitlement"
      contains: "com.apple.developer.kernel.increased-memory-limit"
    - path: "ios/throughput-smoke.log"
      provides: "Measured tok/s from on-device generation in airplane mode"
      contains: "tok/s"
  key_links:
    - from: "ios/mlx-swift-examples/.../ContentView.swift"
      to: ".planning/phases/01-foundation-smoke-tests/base-model.txt"
      via: "modelConfiguration id string matches file contents"
      pattern: "unsloth/gemma-4-(E4B|E2B)-it-UD-MLX-4bit"
---

<objective>
Fork `mlx-swift-examples/LLMEval`, swap the model configuration to the Phase 1 base model (chosen empirically in plan 02), sign the increased-memory-limit entitlement, build to iPhone 17, and verify ≥40 tok/s in airplane mode.

Purpose: this is the Phase 1 H1 leg. It gates everything downstream — plan 04 can't hot-swap an adapter into an app that doesn't run, and plan 05 can't bridge JSContext into a model that doesn't generate.
Output: working LLMEval-fork app on physical iPhone 17 in airplane mode with a measured throughput number.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@PRD_SPEC.md
@CLAUDE.md
@.planning/phases/01-foundation-smoke-tests/01-CONTEXT.md
@.planning/phases/01-foundation-smoke-tests/01-RESEARCH.md
@.planning/phases/01-foundation-smoke-tests/base-model.txt

<interfaces>
From mlx-swift-examples (to be discovered in the fork):
- `ModelConfiguration` struct — holds id of the HF MLX repo to load
- `ModelContainer` / `ModelContext` — generation handle
- `generate(...)` returns an `AsyncStream` (or `AsyncThrowingStream`) of decoded text chunks
- Canonical LoRA reference: `mlx-swift-examples/Tools/llm-tool/LoraCommands.swift`

Operator verified in plan 01 that `mlx-swift-examples` is cloned under `ios/` and that Xcode 16 opens the project with signing green.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch modelConfiguration → Gemma 4 + sign increased-memory-limit entitlement</name>
  <read_first>
    - .planning/phases/01-foundation-smoke-tests/base-model.txt (chosen model id from plan 02)
    - .planning/phases/01-foundation-smoke-tests/01-RESEARCH.md §Xcode Setup, §LLMEval Fork, §P2 (entitlement not signed pitfall)
    - PRD_SPEC.md §8.1, §13.3
    - ios/mlx-swift-examples/Applications/LLMEval/ContentView.swift (or wherever modelConfiguration lives — grep it)
    - ios/mlx-swift-examples/Applications/LLMEval/ModelContainer.swift (if present)
  </read_first>
  <files>
    ios/mlx-swift-examples/Applications/LLMEval/ContentView.swift,
    ios/mlx-swift-examples/Applications/LLMEval/LLMEval.entitlements,
    ios/mlx-swift-examples/Applications/LLMEval/Info.plist
  </files>
  <action>
    1. Read the base model id: `BASE=$(cat .planning/phases/01-foundation-smoke-tests/base-model.txt)`.
    2. Grep the LLMEval application folder for `modelConfiguration` or the existing model id literal:
       `grep -rn "modelConfiguration\\|ModelRegistry\\|huggingface\\|mlx-community\\|unsloth" ios/mlx-swift-examples/Applications/LLMEval/`
    3. Replace the existing model id string with `$BASE` (e.g., `unsloth/gemma-4-E4B-it-UD-MLX-4bit`). If the codebase uses a pre-registered `ModelRegistry` entry (e.g., `ModelRegistry.llama3_2_1B_4bit`), either add a new registry entry for Gemma 4 or construct a literal `ModelConfiguration(id: $BASE)` inline. Verify by reading `mlx-swift-lm` 3.x Models directory: `ls ios/mlx-swift-examples/Libraries/MLXLLM/Models/` — if `Gemma4.swift` is absent, this is a Phase 1 kill-point (RESEARCH §LLMEval Fork step 2); halt and surface to operator.
    4. Add increased-memory-limit entitlement. Preferred path (RESEARCH §Xcode Setup): open `.xcodeproj`, target → Signing & Capabilities → `+ Capability` → "Increased Memory Limit". Fallback (scriptable): open the `LLMEval.entitlements` plist and add:
       ```xml
       <key>com.apple.developer.kernel.increased-memory-limit</key>
       <true/>
       ```
       Save as `LLMEval.entitlements`. In Xcode, ensure the target Build Settings → Code Signing Entitlements points to this file.
    5. Set bundle identifier to `com.hackathon.specialist` (consistent with plan 04's `deploy-adapter.sh`). Record it: echo it into `ios/bundle-id.txt` so plan 04 reads the exact value.
    6. Select the iPhone 17 UDID from plan 01's `derisk-checklist.md` as the active run destination.
    7. Build to the physical iPhone 17 via:
       `xcodebuild -project ios/mlx-swift-examples/mlx-swift-examples.xcodeproj -scheme LLMEval -destination "platform=iOS,id=<UDID>" build`
       If Xcode project is SPM-first, adapt destination flags accordingly. Expected build time 60-180 s.
    8. Verify entitlement is signed into the built product (P2 avoidance):
       `codesign -d --entitlements - $(find ~/Library/Developer/Xcode/DerivedData -name "LLMEval.app" | head -1) 2>&1 | grep "increased-memory-limit"`
       Must return a line containing `com.apple.developer.kernel.increased-memory-limit` and `<true/>`.

    **Escalation (operational fallback):** If Xcode signing/provisioning fails by H1:15, escalate to Tier 3 cassette decision — stop H1 work, confirm pre-recorded Tier 3 cassette plan owner. (Matches escalation pattern in plans 04/05; not a required exit — only fires if signing is hard-blocked by provisioning/cert issues within the H1 budget.)
  </action>
  <verify>
    <automated>BASE=$(cat .planning/phases/01-foundation-smoke-tests/base-model.txt) &amp;&amp; grep -rq "$BASE" ios/mlx-swift-examples/Applications/LLMEval/ &amp;&amp; grep -q "increased-memory-limit" ios/mlx-swift-examples/Applications/LLMEval/LLMEval.entitlements &amp;&amp; test -f ios/bundle-id.txt &amp;&amp; grep -q "com.hackathon.specialist" ios/bundle-id.txt &amp;&amp; codesign -d --entitlements - $(find ~/Library/Developer/Xcode/DerivedData -name "LLMEval.app" -type d | head -1) 2>&amp;1 | grep -q "increased-memory-limit"</automated>
  </verify>
  <acceptance_criteria>
    - Chosen model id from `base-model.txt` appears in at least one `.swift` file under `ios/mlx-swift-examples/Applications/LLMEval/`.
    - `LLMEval.entitlements` contains the increased-memory-limit key set to `<true/>`.
    - `ios/bundle-id.txt` contains `com.hackathon.specialist`.
    - `codesign -d --entitlements` on the built .app reports the entitlement as signed (P2 avoidance).
    - `xcodebuild ... build` exits 0.
    - Documented escape path: if Xcode signing/provisioning fails by H1:15, escalate to Tier 3 cassette decision — stop H1 work, confirm pre-recorded Tier 3 cassette plan owner. (Not a required exit — only logged if triggered.)
  </acceptance_criteria>
  <done>Gemma 4 model plumbed into the fork; entitlement verifiably signed into the built binary; build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Install on iPhone 17, enter airplane mode, measure ≥40 tok/s (PLAT-07)</name>
  <read_first>
    - .planning/phases/01-foundation-smoke-tests/01-RESEARCH.md §Throughput, §P2 (below 40 tok/s debug)
    - PRD_SPEC.md §19.3 acceptance
  </read_first>
  <what-built>
    Xcode build succeeded with Gemma 4 + increased-memory-limit. Operator will now install on physical device, trigger first-launch weight download (if needed), put phone in airplane mode, run one generation, and read tok/s off the screen (or from OSLog).
  </what-built>
  <files>ios/throughput-smoke.log</files>
  <how-to-verify>
    1. Connect iPhone 17 via USB-C. In Xcode, select the device and hit Run (or `xcodebuild -destination "platform=iOS,id=<UDID>" -scheme LLMEval install`).
    2. On first launch, `swift-hf-api-mlx` will download Gemma 4 (~3 GB). Keep iPhone on Wi-Fi until download completes; watch network indicator / Settings → Storage.
    3. Once download is complete, put iPhone into airplane mode (swipe down → airplane icon). Verify Wi-Fi, Bluetooth, Cellular all off.
    4. In the app, enter prompt: `Why is the sky blue?` with `maxTokens=200`.
    5. Observe the generation. If LLMEval UI displays tok/s (most forks do), read it. If not, add 3 lines to ContentView.swift wrapping the generate() call with `CFAbsoluteTimeGetCurrent()` at start/end and NSLog the rate (per RESEARCH §Throughput). Rebuild and retry.
    6. Record the measured tok/s to `ios/throughput-smoke.log`:
       `echo "PLAT-07 tok/s=<N> model=<model-id> airplane=true date=$(date -Iseconds)" > ios/throughput-smoke.log`
    7. Also add a row to `.planning/STATE.md` Decisions Log:
       `| 2026-04-18 H1 | PLAT-07: on-device throughput = <N> tok/s airplane mode | ios/throughput-smoke.log |`
    8. If < 40 tok/s, consult RESEARCH §Throughput debug list (entitlement signing — re-run Task 1 check; wrong quantization — verify model id; KV cache q4). Do NOT pass this checkpoint under 40.
  </how-to-verify>
  <acceptance_criteria>
    - `ios/throughput-smoke.log` exists and contains a line matching `tok/s=` with a numeric value.
    - The numeric value is ≥ 40.
    - `airplane=true` appears in the same line (confirms measurement was in offline mode).
    - `grep PLAT-07 .planning/STATE.md` returns a row.
  </acceptance_criteria>
  <resume-signal>Type the measured tok/s (e.g., "48 tok/s") to confirm PLAT-07 pass, OR describe the blocker if below 40.</resume-signal>
</task>

</tasks>

<verification>
- LLMEval fork builds to iPhone 17 with Gemma 4 base model and signed entitlement.
- On-device generation in airplane mode hits ≥40 tok/s.
- STATE.md has a PLAT-07 row with the measured number.
</verification>

<success_criteria>
PLAT-01 and PLAT-07 complete. H1 kickoff: iPhone has a working specialist shell ready to receive adapters (plan 04) and tool bridges (plan 05).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-smoke-tests/01-03-SUMMARY.md` with: bundle id, measured tok/s, base model id installed on device, any deviations from canonical fork.
</output>
